// Shared grid dimensions for the printer layout, used by both the read-only
// PrinterGrid (dashboard cards, picker modal) and the draggable version on
// the config page, so the two always line up.
export const PRINTER_GRID_COLUMNS = 10;
export const PRINTER_GRID_ROWS = 6;
